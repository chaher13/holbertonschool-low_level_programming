#include "main.h"
/**
 * mul - Multiplication two integers
 * @x: The first number to multiply
 * @y: The second numbernto multiply with first number
 * Return: The result of multiplication between param x and param y
 */

int mul(int x, int y)
{

return (x * y);
}
