#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main(void)
{
int nz;

for (nz = '0'; nz <= '9'; nz++)
putchar(nz);
putchar('\n');

return (0);
}
